enum CategorySize {
	BIG = "big",
	SMALL = "small",
}

export enum Roles {
	SUPER_ADMIN = "super_admin",
	ADMIN = "admin",
	USER = "user",
}

export enum Gender {
	MAN = "man",
	WOMAN = "woman",
	OTHER = "other",
}

export enum OrderStatus {
	PENDING = "pending",
	APPROVED = "approved",
	DELIVERED = "delivered",
	FINISHED = "finished",
	CANCELLED = "cancelled",
}

export enum ProductType {
	AUTHOR = "author",
	IN_STOCK = "in_stock",
}

export { CategorySize };
